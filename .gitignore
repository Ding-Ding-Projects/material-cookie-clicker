node_modules/
dist/
release/
coverage/
.vite/
*.log
.env*
!.env.example
packages/*/dist/

# Throwaway capture profile + seeded save, written by the capture harness. Never committed.
.capture-golden/

# Capture-run scratch: throwaway Electron profiles, seeded saves and staged frames.
captures/tmp/refresh/
