# Contributing

Use a focused branch or linked worktree, keep changes scoped, and add tests plus feature documentation with each behavior change. Run `npm run check` and `npm run build` before submitting. Do not add arbitrary command execution, renderer-side credentials, guessed installer flags, code signing, remote scripts/fonts/images, or mock data presented as real game state.
